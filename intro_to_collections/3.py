# Identify at least 2 differences between lists and tuples. Identify at least 2 ways that lists and tuples are similar.

# a list is mutable and a tuple is not. a list uses '[]' and a tuple uses '()'
# a list can store any data type just like a tuple can. a list and a tuple both use ordered indexing.